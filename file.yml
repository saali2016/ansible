 - hosts: all
   gather_facts: false
   tasks:
   - name: file doesnt exist, but i dont care .. file plugin itself warns anyways ...
     debug: msg="{{ lookup('file', '/var/lib/awx/projects/_12__case/template.csv', errors='ignore') }}"
   
   
   
   #tasks:
   #- name: test 
   #  shell: "pwd"
   
   #- name: File Stat
    # file:
     #  path: ./template.yml
      # mode: '0446'
       #owner: root
